{
  "name": "crypto_trading_app",
  "version": "1.0.0",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "setup": "git clone https://github.com/flutter/flutter.git && export PATH=\"$PATH:`pwd`/flutter/bin\"",
    "vercel-build": "npm run setup && flutter pub get && flutter build web --release --web-renderer html"
  }
}
